{
  "scenarioId": "cli-entry-routing",
  "description": "CLI correctly routes all registered commands and exits 1 on unknown commands",
  "capabilitiesCovered": ["cli-entry"],
  "steps": [
    "Run: infernoflow --version → prints semver string",
    "Run: infernoflow --help → prints command list without error",
    "Run: infernoflow unknown-cmd → exits 1 with 'Unknown command' message",
    "Run: infernoflow status (in a non-inferno dir) → exits with clear error, not a crash"
  ],
  "expects": [
    "--version output matches package.json version",
    "--help lists all COMMAND_DESCRIPTIONS entries",
    "Unknown commands exit 1 and suggest --help",
    "No unhandled promise rejections on any known command"
  ]
}
