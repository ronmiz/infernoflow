{
  "scenarioId": "impact-blast-radius",
  "description": "Blast radius analysis correctly traverses graph and assigns risk levels",
  "capabilitiesCovered": ["capability-impact"],
  "steps": [
    "Run: infernoflow impact cli-entry → risk should be CRITICAL (frozen + has dependents)",
    "Run: infernoflow impact ai-provider-router → risk should be CRITICAL",
    "Run: infernoflow impact capability-demo → risk should be LOW (no dependents)",
    "Run: infernoflow impact capability-impact --check → should exit 1 (stable with dependents = HIGH)",
    "Run: infernoflow impact cli-entry --json → validate JSON shape"
  ],
  "expects": [
    "frozen cap with dependents → CRITICAL",
    "stable cap with no dependents → LOW",
    "--check exits 1 on HIGH or CRITICAL",
    "JSON output includes: capId, risk, direct, transitive, scenarios"
  ]
}
